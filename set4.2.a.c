#include <stdio.h>
#include<string.h>

void main()
{
    char a[50];
    int i=0,word=1;
    printf("enter string");
    gets(a);
    while(str[i])!='\0')
    {
        if(str[i]==''||str[i]==1)
        {
            word++;
        }
            i++;
        }
        
    }
        printf("total words =%d");

    getch();
}
